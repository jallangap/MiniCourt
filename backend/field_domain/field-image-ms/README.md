# Field Image Microservice

This microservice handles the update of image URLs for registered sports fields.

## Features
- Update image URL for a field

## Endpoint

**PUT /api/fields/image/:id**

```json
{
  "imageUrl": "https://example.com/image.jpg"
}
